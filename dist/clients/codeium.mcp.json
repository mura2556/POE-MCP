{
  "mcpServers": [
    {
      "name": "poe-mcp",
      "command": "{{ABS_PATH}}/bin/poe-mcp",
      "args": [
        "serve",
        "--transport",
        "stdio"
      ]
    }
  ],
  "notes": "Enable MCP bridge via Codeium Labs or Sourcegraph Orion."
}